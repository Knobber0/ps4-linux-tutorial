# Node / VitePress build garbage
node_modules/
.env
.env.local
.dist/
.vitepress/dist/
.vitepress/cache/

# Git internals (never commit these)
.git/
.git/logs/
.git/hooks/
.git/info/
.git/objects/
.git/refs/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
*~

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Local overrides (you probably have some secret sauce)
local.yml
local.conf
hyprland.local.conf

# VitePress public assets you regenerate
docs/public/screenshots/
docs/public/scripts/

# Built site (you push to GitHub Pages anyway)
docs/.vitepress/dist/
docs/.vitepress/cache/

# npm / pnpm / yarn crap
package-lock.json
yarn.lock
pnpm-lock.yaml
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: ignore the entire built docs folder if you deploy via CI
# docs/.vitepress/dist/

# If you ever compile kernels or big binaries locally
/build/
/result/
/bzImage
/initrd
/*.img
/*.iso

# Temporary screenshots or test folders
tmp/
temp/
test/

# GitHub Actions cache (sometimes huge)
.github/workflows/.github/

# Nix flakes (if anyone uses it on PS4 Linux)
/result
/shell.nix
/flake.nix.lock
